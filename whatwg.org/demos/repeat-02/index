<!DOCTYPE html PUBLIC "-//WHATWG//NONSGML HTML5//EN">
<html>
 <head>
  <title>Repetition Model Demo 02</title>
  <style type="text/css">
   strong { color: red; }
   table { border: hidden; border-collapse: collapse; }
   thead { border-bottom: solid; }
   td, th { border-left: solid; }
  </style>
 </head>
 <body>
  <p>In a Web Forms 2.0 UA, the table below will be populated with
  four rows of data. In legacy UAs, messages will appear saying that
  the data could not be filled. Generally, for backwards compatibility
  this sort of thing is best done using server-side templating
  tools.</p>
  <form data="results.xml">
   <p repeat="template"><strong>Your user agent does not support
   repetition templates, so the data will not be
   prefilled.</strong></p>
   <p><output name="message"><strong>Your user agent does not support
   form prefilling, so the data will not be
   prefilled.</strong></output></p>
   <table>
    <caption>Game results</caption>
    <thead>
     <tr>
      <th> Player Name </th>
      <th> Score </th>
     </tr>
    </thead>
    <tbody>
     <tr repeat="template" id="player">
      <td> <output name="player[player].name"></output> </td>
      <td> <output name="player[player].score"></output> </td>
     </tr>
    </tbody>
   </table>
  </form>
 </body>
</html>